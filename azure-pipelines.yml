trigger:
  batch: true
  branches:
    include:
      - master
      - refs/tags/v*

pr:
  - master

variables:
  PYTHONUNBUFFERED: 1
  YARN_CACHE_FOLDER: $(Pipeline.Workspace)/.yarn

jobs:
  - template: ci/job.test.yml
